# Base URL of the gptloop backend. The browser never calls this directly — Vite proxies
# the app's own /api/* routes to it, so localhost resolution happens on the machine running
# the dev server (needed when the UI is opened via a remote proxy URL).
GPTLOOP_API_URL=http://localhost:8787

# Dev server port for the frontend.
VITE_PORT=5173
